services:
  app:
    build: ./
    primary: true
    exposePort: 3000
    environment:
      - name: MOCK_DEPLOYMENT
        value: 1
  api:
    build: ./
    externalUrl: https://github.com/voyage-playground/user-app-api
    exposePort: 3001
    externalBranchDefault: foobar
    environment:
      - name: PORT
        value: '3001'
